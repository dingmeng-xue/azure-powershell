<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PsModuleName>Websites</PsModuleName>
  </PropertyGroup>

  <Import Project="$(MSBuildThisFileDirectory)..\..\Az.props" />

  <PropertyGroup>
    <AssemblyName>$(AzAssemblyPrefix)$(PsModuleName).Helper</AssemblyName>
    <RootNamespace>$(AzAssemblyPrefix)$(PsModuleName).Helper</RootNamespace>
  </PropertyGroup>
</Project>